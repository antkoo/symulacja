/**
 * Main function. Starts the simulation by creating the {@link Board}.
 */
public class Main {
    /**
     * Main function, used to launch the program.
     * @param args arguments
     */
    public static void main(String[] args) {
        new Board();
    }
}